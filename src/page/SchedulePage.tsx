"use client";

import { Box, Typography } from "@mui/material";

export const SchedulePage = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4">予定</Typography>
        </Box>
    );
};
