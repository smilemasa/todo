import { Box, Typography } from "@mui/material";

export const ArchivePage = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4">アーカイブ</Typography>
        </Box>
    );
};
