import { Box, Typography } from "@mui/material";

export const SettingsPage = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4">設定</Typography>
        </Box>
    );
};
